function invertirNumero(numero) {
    let separado = numero.split('');
    let arreglo = [];
    arreglo.push(numero);
    console.log(arreglo)
    let convertido = numero.toString();
    convertido.split();
    
}

invertirNumero(20);